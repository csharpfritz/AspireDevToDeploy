{
    "version": "0.2.0",
    "compounds": [
        {
            "name": "Run all",
            "configurations": [
                "Run api",
                "Run web"
            ]
        }
    ],
    "configurations": [
        {
            "name": "Run AppHost",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}\\AppHOst\\AppHost.csproj"
        },

    ]
}